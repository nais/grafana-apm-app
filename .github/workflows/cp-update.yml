name: Create Plugin Update

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 1 * *' # run once a month on the 1st day

permissions:
  contents: write
  pull-requests: write

# To use this workflow please create a fine-grained personal access token for your repository with:
# `contents: read and write`
# `pull requests: read and write`
# `workflows: read and write`

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: grafana/plugin-actions/create-plugin-update@a66a1c96cdbb176f9cccf10cf23593e250db7cce # ratchet:grafana/plugin-actions/create-plugin-update@create-plugin-update/v1.1.0
        with:
          # Make sure to save the token in your repository secrets as `GH_PAT_TOKEN`
          token: ${{ secrets.GH_PAT_TOKEN }}
