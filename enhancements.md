# Service Overview

- Use propper icons instead of emojis
- Default to 50 services per page
- Filter by namespace (team)

# Service Details

- Use full width and height (this goes for all tabs; traces, logs etc.)

# Dependencies

- Depencency list and service graph does not match?

# General

- No easy way to navigate back to service list
- Should there be a team/namespace specific view? (e.g. show only services in a specific team/namespace)
